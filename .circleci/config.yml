version: 2.1

orbs:
  python: circleci/python@2.0.1

jobs:
  build-and-test:
    docker:
      - image: cimg/python:3.10.2
    steps:
      - checkout
      - run: python3 -m unittest TestTriangle.py

workflow:
  sample:
    jobs:
      - build-and-test